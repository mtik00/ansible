---
- name: Run all of the roles
  hosts: localhost
  connection: local
  vars_files:
    - .secrets/sudo-password.yml
  roles:
    - role: git
      tags: [ git ]
    - role: dotfiles
      tags: [ dotfiles ]
    - role: zsh
      tags: [ zsh ]
    - role: home-bin
      tags: [ home-bin ]
