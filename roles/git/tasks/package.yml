---

- name: git PPA repo
  become: true
  ansible.builtin.apt_repository:
    repo: ppa:git-core/ppa
    codename: "{{ ansible_lsb.codename }}"
    state: present

- name: git package
  become: true
  ansible.builtin.apt:
    name: git
    state: present
